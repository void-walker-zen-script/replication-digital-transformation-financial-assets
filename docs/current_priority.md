# 当前项目优先级

## 第一阶段：完成真实数据构造

准备合法获得的年报文本、财务报表科目和公司治理字段，依次构造 `digital`、`fin1`、
`fin2`、`Size`、`LEV`、`ROA`、`ListAge`、`TOP1` 和 `Indep`，最终合并为
`data/processed/panel_for_replication.csv`。

## 第二阶段：运行 Table 2 描述性统计

在真实面板通过字段、主键、缺失值和单位检查后，计算描述性统计并核对样本结构。

## 第三阶段：运行 Table 3 基准回归

分别以 `fin1` 和 `fin2` 为被解释变量，运行企业固定效应和年份固定效应模型。

## 第四阶段：对比原文结果

对比样本量、变量分布、系数方向、数量级、标准误和显著性，并如实记录差异。

## 第五阶段：后续扩展

只有在数据构造、Table 2、Table 3 和原文对比完成后，才讨论后续扩展。

**当前不进入 DID、机制分析、异质性分析。**

